AccountTest
        setBalance() getBalance()
        X=getBalance() setBalance(X) // related unification

AllocateVector
        X=getFreeBlockIndex() markAsAllocatedBlock(X)

ArithmeticDatabase
        X=get_max_key() insert(X,_) // related unification
        iterator() get_max_key()

ConnectionTest
        isConnected() send()
        resetSocket() resetCounter()

Coord03
        getX() getY()
        getY() getX()
        setX() setY()
        setY() setX()

Coord04
        resetX() resetY()
        resetY() resetX()
        getX() getY() // swap (implicit calls)
        getY() getX() //          "

Elevator
        checkUp(X) claimUp(_,X)
        checkDown(X) claimDown(_,X)

Jigsaw
        loopupEntry() checkClosed()

Knight
        get_solution(X)  set_solution(X,_)

Local
        X=getValue() setValue(X) // related unification

MyStringBuffer
        X=length() getChars(_,X,_,_)

NASA
        setValue(_,X) setAchieved(_,X)

Store
        hasOrders() treatOrder()

UnderReportingTest
        inc() inc()*

VectorFail
        max() min()
        min() max()
